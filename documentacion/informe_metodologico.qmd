---
title: "Student Analytics UA — Informe metodológico"
subtitle: "Fuentes, insumos, estadísticas descriptivas y metodología de la fase previa al acceso a datos institucionales"
author: "Proyecto Analítica de Estudiantes — Facultad de Administración y Negocios"
date: today
lang: es
format:
  html:
    toc: true
    toc-title: "Contenidos"
    toc-depth: 3
    toc-location: left
    number-sections: true
    theme: cosmo
    css: estilos.css
    include-before-body: encabezado.html
    include-after-body: pie.html
    code-fold: true
    code-summary: "Ver código"
    df-print: kable
    fig-cap-location: top
    tbl-cap-location: top
    embed-resources: true

  # PDF via Typst. No requiere LaTeX: Quarto trae Typst incorporado, asi que
  # el informe se genera en cualquier maquina con Quarto y sin instalar una
  # distribucion de 200 MB. Las claves de estilo del bloque html (css,
  # include-before-body) son propias de ese formato; el equivalente aca es
  # encabezado.typ, que pone el logo como encabezado corrido.
  typst:
    toc: true
    toc-title: "Contenidos"
    toc-depth: 3
    number-sections: true
    section-numbering: "1.1.1"
    papersize: a4
    fontsize: 10pt
    margin:
      x: 2.2cm
      top: 3.0cm      # el encabezado con logo vive dentro de este margen
      bottom: 2.2cm
    df-print: kable
    fig-cap-location: top
    tbl-cap-location: top
    include-in-header: encabezado.typ
execute:
  echo: false
  warning: false
---

```{python}
#| label: setup
import json
from pathlib import Path
import pandas as pd

DATOS = Path("datos")

def leer(nombre):
    p = DATOS / f"{nombre}.csv"
    return pd.read_csv(p) if p.exists() else None

resumen = json.loads((DATOS / "resumen.json").read_text(encoding="utf-8")) \
    if (DATOS / "resumen.json").exists() else {}

def pct(x, d=1):
    return "—" if x is None else f"{x:.{d}%}"
```

::: {.callout-important}
## Ninguna cifra de este informe proviene de datos internos de la Universidad Autónoma

Al momento de escribir este documento **el proyecto no tiene acceso a Banner ni a
Canvas**. Todo lo que se reporta proviene de tres orígenes: datos abiertos del
Ministerio de Educación de Chile, un conjunto de datos público del Reino Unido, y
un generador sintético construido para este proyecto.

La única excepción son las cifras de retención de la propia Universidad Autónoma
(§ @sec-retencion), que **sí son reales** pero provienen de las bases abiertas de
Mineduc, no de sistemas internos.
:::

# Resumen ejecutivo

Este informe documenta la fase previa al acceso a los datos institucionales. Su
propósito no fue construir un modelo, sino **dejar un pipeline cuyo contrato de
entrada calce con Banner y Canvas lo bastante bien como para que la migración a
datos reales sea un cambio de configuración**, y producir la evidencia que
justifica solicitar ese acceso.

Los tres resultados principales:

1. **La retención real de la Universidad Autónoma se puede calcular hoy**, desde
   datos abiertos, cruzando las bases de matrícula por el identificador MRUN. En
   el área de Administración y Comercio la cohorte 2024 retuvo **84,1%** en la
   misma carrera. Entre sedes hay una brecha de **14 puntos**.

2. **Todo el expediente escolar previo predice poco.** Con 135.050 ingresantes
   universitarios y validación temporal, un modelo con NEM, ranking, notas de
   enseñanza media, los cinco puntajes PAES, dependencia del colegio,
   **asistencia mensual de 4° medio, decil de ingreso oficial, condición de
   prioritario y tipo de financiamiento** alcanza **AUC 0,636**. Solo la ficha
   de admisión llega a 0,617; todo el resto suma 0,019.

3. **Los datos de comportamiento superan a ese expediente antes del primer mes.**
   Sobre datos reales de la Open University, un baseline conductual alcanza
   **AUC 0,697 en la semana 4** — con el 10% del curso transcurrido y sin
   registro de asistencia.

De ahí se sigue el argumento del proyecto: el valor no está en lo que ya se sabe
del estudiante al matricularse, sino en lo que ocurre durante el semestre.

---

# Mandato y alcance {#sec-mandato}

## Insumos documentales

El proyecto se apoya en tres documentos, incluidos en `docs/`:

| Documento | Origen | Qué aporta |
|---|---|---|
| *2026 Postulación FPDA BA v3* | Formulario Fondo de Desarrollo Académico, Universidad Autónoma de Chile | El mandato formal: alcance, compromisos e indicadores por mes |
| *AT_informe-final-2* | Hofflinger, A. (2020). Universidad de Los Lagos | Benchmark metodológico; el hallazgo que define el problema |
| *Actualiza-Titulos-Grados* | Subsecretaría de Educación Superior | Marco sistémico: masificación, heterogeneidad, trayectorias flexibles |

## Compromisos formales

La postulación adjudicada compromete plazos concretos:

| Compromiso | Plazo |
|---|---|
| Permisos de acceso a los datos consolidados por la VRA | antes del mes 2 |
| Segmentos de estudiantes obtenidos | antes del mes 3 |
| ≥3 algoritmos entrenados, modelo priorizado 1 | antes del mes 4 |
| ≥3 algoritmos entrenados, modelo priorizado 2 | antes del mes 6 |

Quedan explícitamente **fuera de alcance** las acciones preventivas de las
direcciones de carrera y cualquier implementación dentro de Banner.

## El hallazgo que define el problema

El informe de la Universidad de Los Lagos es el antecedente más relevante. Su
resultado central:

> Con solo datos previos al ingreso (género, etnia, NEM, PSU, ingreso familiar,
> dependencia), para alcanzar al 80% de los futuros desertores había que
> intervenir al **80% de los estudiantes**. Incorporando las notas del primer
> semestre, capturar el 60% requería solo el **20%**.

Y su recomendación explícita fue mejorar la predicción durante el primer semestre
incorporando **notas parciales, asistencia y uso de plataformas virtuales**. Este
proyecto es la continuación operativa de esa recomendación.

---

# Hallazgos principales {#sec-hallazgos}

Esta sección reúne lo que el proyecto encontró. Cada cifra sale de
`documentacion/datos/hallazgo_*.csv`, que genera `scripts/build_findings.py`
desde los mismos artefactos que alimentan el tablero: **ninguna está escrita a
mano**, y la sección «Hallazgos» de la aplicación muestra exactamente estos
mismos números. Las secciones posteriores desarrollan cada punto.

## 1. El valor no está en lo que ya se sabe del estudiante

```{python}
#| label: fig-argumento
#| fig-cap: "Poder predictivo del expediente previo al ingreso frente al comportamiento observado en la semana 4"
import matplotlib.pyplot as plt

TINTA, AZUL, NARANJA, GRIS = "#3D3935", "#2a78d6", "#eb6834", "#8a8880"
plt.rcParams.update({"font.size": 9, "axes.edgecolor": GRIS,
                     "axes.labelcolor": TINTA, "text.color": TINTA,
                     "xtick.color": GRIS, "ytick.color": GRIS,
                     "axes.spines.top": False, "axes.spines.right": False})

d = leer("hallazgo_argumento_central")
if d is not None and len(d) >= 2:
    fig, ax = plt.subplots(figsize=(7.2, 1.9))
    etiquetas = ["Todo el expediente\nprevio al ingreso", "Conducta observada\nen la semana 4"]
    ax.barh(etiquetas, d["auc"], color=[GRIS, AZUL], height=.55)
    ax.axvline(.5, color=GRIS, ls="--", lw=1)
    for y, v in enumerate(d["auc"]):
        ax.text(v + .004, y, f"{v:.3f}", va="center", fontweight="bold")
    ax.set_xlim(.5, .75); ax.set_xlabel("AUC")
    # Va en el hueco entre las dos barras: abajo se monta sobre el tick del
    # 0,50 y arriba la recorta el borde de la figura.
    ax.annotate("azar", xy=(0.008, 0.5), xycoords="axes fraction",
                color=GRIS, fontsize=8, va="center")
    ax.invert_yaxis(); fig.tight_layout(); plt.show()
```

```{python}
#| label: tbl-argumento
#| tbl-cap: "Los dos resultados, uno junto al otro"
d = leer("hallazgo_argumento_central")
if d is not None:
    d["auc"] = d["auc"].map("{:.3f}".format)
    d["n"] = d["n"].map(lambda v: "—" if pd.isna(v) else f"{v:,.0f}")
    d = d[["momento", "detalle", "n", "auc", "anticipacion"]]
    d.columns = ["Momento", "Qué se sabe", "n", "AUC", "Margen para actuar"]
d
```

Todo lo que el Estado de Chile sabe de un estudiante **antes de que pise la
universidad** —NEM, ranking, pruebas de admisión, dependencia del colegio,
asistencia mensual de cuarto medio, decil de ingreso oficial, condición de
prioritario, tipo de financiamiento— se queda por debajo de un modelo que solo
mira **cuatro semanas de comportamiento** dentro del curso, y que ni siquiera
dispone de asistencia.

::: {.callout-important}
## La consecuencia operativa

Un sistema de alerta temprana construido sobre la ficha de admisión ordena poco
mejor que el azar y, sobre todo, **no cambia durante el semestre**: clasifica al
estudiante el día que se matricula y lo deja ahí. El comportamiento observado sí
se mueve, y por eso admite una intervención.

Ese es el caso para pedir acceso a Banner y Canvas. Detalle en @sec-argumento.
:::

## 2. La UA está sobre el sistema en continuidad, y la brecha entre sedes es interna

```{python}
#| label: fig-retencion
#| fig-cap: "Continuidad en la misma universidad al año siguiente: UA frente al resto del sistema, 19 cohortes"
d = leer("hallazgo_serie_retencion")
if d is not None:
    fig, ax = plt.subplots(figsize=(7.2, 2.8))
    ax.plot(d["cohorte"], 100 * d["retencion_misma_universidad_ua"], "-o",
            color=AZUL, lw=2, ms=4, label="Universidad Autónoma")
    ax.plot(d["cohorte"], 100 * d["retencion_misma_universidad_sistema"], "-o",
            color=GRIS, lw=2, ms=4, label="Resto del sistema")
    ax.set_ylabel("Continuidad (%)"); ax.set_xlabel("Cohorte de ingreso")
    ax.set_xticks(d["cohorte"][::2]); ax.legend(frameon=False, loc="lower right")
    fig.tight_layout(); plt.show()
```

La UA supera al resto del sistema en **todas** las cohortes de la serie. La
referencia excluye a la UA, de modo que una institución grande no atenúe su
propia comparación.

```{python}
#| label: tbl-sede
#| tbl-cap: "Continuidad por sede — área del piloto, última cohorte disponible"
d = leer("hallazgo_retencion_sede")
if d is not None:
    d = d[["nomb_sede", "n", "retencion_misma_carrera",
           "retencion_misma_universidad", "retencion_sistema"]].copy()
    for c in d.columns[2:]:
        d[c] = d[c].map("{:.1%}".format)
    d.columns = ["Sede", "n", "Misma carrera", "Misma universidad", "Cualquier institución"]
d
```

::: {.callout-tip}
## La sede debe ser variable del modelo, no filtro del tablero

La distancia entre la sede con mayor y menor continuidad supera los diez puntos
porcentuales, con tamaños que no admiten leerla como ruido. Una institución que
está **sobre** el promedio nacional contiene internamente un rango que el
promedio esconde: es exactamente el tipo de heterogeneidad que un modelo debe
capturar y un indicador agregado no.
:::

## 3. La UA es del montón en selectividad, y no se mueve

```{python}
#| label: tbl-selectividad
#| tbl-cap: "Posición de la UA en selectividad de admisión, por cohorte"
d = leer("hallazgo_selectividad")
if d is not None:
    t = pd.DataFrame({
        "Cohorte": d["cohorte"].astype(str), "Prueba": d["instrumento"],
        "Puntaje UA": d["ua_puntaje"].map("{:,.0f}".format),
        "Media del sistema": d["sistema_puntaje_medio"].map("{:,.0f}".format),
        "Percentil UA": d["ua_percentil"].map("{:.0%}".format),
        "Supera a": d["posicion_ua"].map("{:.0%}".format),
        "Universidades": d["universidades"],
        "Cobertura UA": d["ua_cobertura"].map("{:.0%}".format)})
    d = t
d
```

La UA **no recibe estudiantes mejores ni peores que el promedio** del sistema, y
esa posición es estable en las cinco cohortes con dato. El hallazgo importa
porque cierra una explicación fácil: sus resultados de continuidad y titulación
no se explican por el perfil de entrada.

::: {.callout-warning}
## Dos advertencias que la cifra no lleva encima

**El puntaje crudo no cruza 2023.** La Prueba de Transición se medía entre 150 y
850 y la PAES entre 100 y 1000, así que la columna «Puntaje UA» salta unos noventa
puntos entre 2022 y 2023 sin que haya cambiado nada. El percentil nacional sí es
comparable, y es la columna que debe leerse en serie. Ver @sec-paes-escala.

**Antes de 2021 no hay dato.** La PSU no está publicada como microdato abierto,
así que catorce de las diecinueve cohortes del benchmark se comparan sin
selectividad.
:::

## 4. Los dos indicadores de titulación apuntan en direcciones opuestas

```{python}
#| label: fig-contraste
#| fig-cap: "Correlación de cada indicador de titulación con la selectividad de admisión y con los años de acreditación"
d = leer("hallazgo_contraste_titulacion")
if d is not None:
    refs = [("paes_percentil_promedio", "Selectividad de admisión"),
            ("acreditacion_anios", "Años de acreditación")]
    refs = [(c, t) for c, t in refs if c in d.columns]
    fig, axes = plt.subplots(1, len(refs), figsize=(7.2, 2.4), sharey=True)
    axes = axes if len(refs) > 1 else [axes]
    corto = [i.replace(" · ", "\n") for i in d["indicador"]]
    for ax, (col, titulo) in zip(axes, refs):
        colores = [NARANJA if t == "Transversal" else AZUL for t in d["tipo"]]
        ax.barh(corto, d[col], color=colores, height=.6)
        ax.axvline(0, color=GRIS, lw=1)
        for y, v in enumerate(d[col]):
            ax.text(v + (.02 if v >= 0 else -.02), y, f"{v:+.2f}", va="center",
                    ha="left" if v >= 0 else "right", fontsize=8)
        ax.set_xlim(-.6, .6); ax.set_title(titulo, fontsize=9, loc="left")
        ax.set_xlabel("Correlación")
    axes[0].invert_yaxis(); fig.tight_layout(); plt.show()
```

Sobre **las mismas universidades**, la titulación transversal correlaciona en
sentido contrario a la titulación por cohorte. No es ruido: miden cosas
distintas.

- **La transversal mide velocidad, no logro.** Cuenta cuánto se demoró quien *sí*
  se tituló, y eso lo determina la mezcla de carreras: una universidad con mucha
  ingeniería aparece abajo porque esos programas se alargan, no porque enseñe
  peor. Por eso correlaciona **negativo** con selectividad y con acreditación.
- **La de cohorte mide cuántos llegan.** Toma a quienes entraron y los busca en
  las bases de titulados por MRUN. Correlaciona **positivo** con ambas
  referencias, que es como debe comportarse un indicador de resultado.

::: {.callout-important}
## Consecuencia para el uso institucional

La titulación oportuna dejaba a la UA en el percentil 94 del sistema. **Ese
número no debe presentarse como logro**: es en buena parte composición de oferta.
El indicador por cohorte —donde la UA marca 61,4% frente a una mediana de
50,9%— es el que sostiene una afirmación sobre desempeño. Ver @sec-cohortes.
:::

## 5. Las trampas de datos que cambiaron un resultado

Esta es la parte del trabajo que no aparece en ningún gráfico y es la que
sostiene todo lo demás. Cada fila es un error que los datos inducen con toda
naturalidad y que habría producido una conclusión falsa.

```{python}
#| label: tbl-trampas
#| tbl-cap: "Trampas encontradas, con el efecto medido de no corregirlas"
d = leer("hallazgo_trampas")
if d is not None:
    d.columns = ["Fuente", "Qué trae el dato", "Qué pasa si no se corrige", "Estado"]
d
```

«Límite permanente» significa que no hay corrección posible con datos abiertos:
es una restricción de la fuente, no del pipeline.

## 6. Qué aportó cada fuente

```{python}
#| label: tbl-fuentes-aporte
#| tbl-cap: "Cobertura y aporte de cada fuente al perfil institucional"
d = leer("hallazgo_fuentes")
if d is not None:
    d.columns = ["Fuente", "Grano", "Cobertura", "Qué aporta", "Variables en el perfil"]
d
```

El conteo de variables se deriva del perfil generado, no de una lista escrita a
mano: si se incorpora una fuente nueva, la cifra se mueve sola.

---

# Fuentes de datos {#sec-fuentes}

## Fuentes utilizadas

| Fuente | Grano | Licencia | Rol en el proyecto |
|---|---|---|---|
| [Matrícula en Educación Superior](https://datosabiertos.mineduc.cl/matricula-en-educacion-superior/) 2007–2026 | Individual (MRUN) | Datos abiertos de gobierno | Retención real UA; población de los modelos; cohortes de ingreso del seguimiento longitudinal |
| [Titulados en Educación Superior](https://datosabiertos.mineduc.cl/titulados-en-educacion-superior/) 2007–2025 | Individual (MRUN) | Datos abiertos de gobierno | Titulación de la promoción que egresa y titulación por cohorte de ingreso |
| [Pruebas de admisión (PDT y PAES)](https://datosabiertos.mineduc.cl/pruebas-de-admision-a-la-educacion-superior/) 2021–2026 | Individual (MRUN) | Datos abiertos de gobierno | Variables previas al ingreso; selectividad de admisión |
| [Asistencia anual por estudiante](https://datosabiertos.mineduc.cl/asistencia-anual-por-estudiante/) 2022, 2023 | Individual (MRUN + RBD), tasa mensual | Datos abiertos de gobierno | Asistencia de enseñanza media y su pendiente |
| [Alumnos SEP](https://datosabiertos.mineduc.cl/alumnos-preferentes-prioritarios-y-beneficiarios-sep/) 2022, 2023 | Individual (MRUN + RBD) | Datos abiertos de gobierno | Vulnerabilidad individual: prioritario / preferente |
| [Asignaciones de Becas y Créditos](https://datosabiertos.mineduc.cl/asignaciones-de-becas-y-creditos-en-educacion-superior/) 2023, 2024 | Individual (MRUN) | Datos abiertos de gobierno | Quintil, decil DFE, gratuidad, FSCU, beca de arancel |
| [OULAD](https://analyse.kmi.open.ac.uk/open_dataset) | Estudiante × módulo × semana | CC-BY 4.0 | Validar la metodología con datos reales de comportamiento |
| [Canvas Data 2 — esquema](https://developerdocs.instructure.com/services/dap/dataset) | Documentación | Pública | Nombres reales de tablas y columnas |
| [Banner ODS Handbook](https://tech.wayne.edu/depdocs/banner-ods-handbook-8-4.pdf) | Documentación | Publicada por Wayne State University | Nombres reales de tablas Banner |
| Generador sintético | Estudiante × asignatura × semana | Propio | Forma UA completa: sedes, carreras, asistencia, escala 1,0–7,0 |

**Cita de OULAD:** Kuzilek, J., Hlosta, M., & Zdrahal, Z. (2017). Open University
Learning Analytics dataset. *Scientific Data*, 4, 170171.

## Sobre el identificador MRUN

Las bases de Mineduc están desagregadas **a nivel individual** y usan MRUN, un
identificador ficticio pero **estable entre bases y años**, que permite seguir la
trayectoria de una persona desde prekínder hasta doctorado. Chile es de los pocos
países OCDE que publica esto abiertamente.

::: {.callout-warning}
## MRUN no se puede unir a Banner

MRUN es un RUT enmascarado y no existe tabla de equivalencia pública. **Estas
bases no pueden generar features de estudiantes concretos de la UA.** Sirven para
calibrar, para benchmarking y para argumentar — no para modelar individuos.
:::

## Fuentes identificadas y no utilizadas todavía

Unibles a estudiantes de la UA por claves **no personales** (RBD del colegio de
origen, comuna), y por tanto candidatas legítimas a features:

| Fuente | Llave | Feature potencial |
|---|---|---|
| [JUNAEB — IVE](http://junaebabierta.junaeb.cl/catalogo-de-datos/indicadores-de-vulnerabilidad/) | RBD | Índice de vulnerabilidad del colegio (0–100) |
| Agencia de Calidad — SIMCE | RBD | Rendimiento histórico del establecimiento |
| [Rendimiento por estudiante](https://datosabiertos.mineduc.cl/rendimiento-por-estudiante-2/) | MRUN + RBD | Notas y promoción año a año en enseñanza media |
| [Matrícula longitudinal](https://datosabiertos.mineduc.cl/matricula-longitudinal/) | MRUN | Trayectoria escolar ya encadenada por el Mineduc |
| [Notas de enseñanza media y percentil](https://datosabiertos.mineduc.cl/notas-de-ensenanza-media-y-percentil-jovenes/) | MRUN | NEM y percentil de egreso; podría cubrir cohortes anteriores a 2021 |
| [Postulaciones a Becas y Créditos](https://datosabiertos.mineduc.cl/postulaciones-a-becas-y-creditos-en-educacion-superior/) | MRUN | Datos FUAS: composición del hogar, ingresos declarados |
| Matrícula ES (calculado) | RBD | Persistencia en educación superior de los egresados de ese colegio |
| CASEN | comuna | Contexto socioeconómico |

El catálogo completo de `datosabiertos.mineduc.cl` tiene **48 conjuntos de
datos**. La prioridad de incorporación debería guiarse por el resultado de la
ablación (§ @sec-resultados): las variables previas al ingreso ya mostraron
rendimientos decrecientes, así que conviene invertir el esfuerzo en los datos
institucionales antes que en más fuentes públicas.

Ver § @sec-limitaciones para la razón por la que su incorporación requiere
cuidado.

## Fuentes de contexto (solo agregadas)

[CNED INDICES](https://www.cned.cl/indices), informes SIES,
[mifuturo.cl](https://www.mifuturo.cl/bases-de-datos/),
[datos.gob.cl](https://datos.gob.cl/group/educacion). Útiles para benchmarking y
acreditación; no aportan al modelamiento.

---

# Estadísticas descriptivas por fuente {#sec-descriptivas}

Una subsección por fuente, con su grano, su cobertura y las variables que el
proyecto efectivamente usa. Todas las cifras provienen de `documentacion/datos/`,
que `scripts/descriptive_stats.py` regenera desde las bases originales.

## Matrícula en Educación Superior (Mineduc / SIES)

```{python}
#| label: tbl-mineduc-volumen
#| tbl-cap: "Volumen de las bases de matrícula en educación superior"
vol = leer("mineduc_volumen")
if vol is not None:
    vol.columns = ["Año", "Filas"]
    vol["Filas"] = vol["Filas"].map("{:,.0f}".format)
vol
```

```{python}
#| label: mineduc-resumen
from IPython.display import Markdown
m = resumen.get("mineduc", {})
Markdown(f"""
De la base 2024 ({m.get('matricula_total_2024', 0):,} filas), la Universidad
Autónoma registra **{m.get('ua_pregrado_2024', 0):,} matriculados de pregrado**,
de los cuales **{m.get('ua_cohorte_2024', 0):,} son cohorte de ingreso 2024**.

Sedes identificadas: {', '.join(m.get('ua_sedes', []))}.
""")
```

```{python}
#| label: tbl-ua-carreras
#| tbl-cap: "Matriculados 2024 de la UA en el área Administración y Comercio"
c = leer("mineduc_ua_admin_por_carrera")
if c is not None:
    c.columns = ["Carrera", "Matriculados 2024"]
c
```

## Pruebas de admisión PAES (Mineduc / DEMRE)

**Grano:** una fila por inscrito, con `MRUN` y `RBD` del establecimiento de
egreso. 130 columnas. Proceso de admisión 2024.

En esta base **el valor 0 significa "no rindió esa prueba", no "obtuvo cero"**.
Tratarlo como puntaje desplaza todas las medias hacia abajo y arruina cualquier
modelo; en el pipeline se convierte explícitamente a valor ausente.

```{python}
#| label: tbl-paes-desc
#| tbl-cap: "Puntajes y expediente escolar — PAES 2024 (excluyendo a quienes no rindieron)"
d = leer("paes_descriptivas")
if d is not None:
    d["rindio"] = d["rindio"].map("{:.1%}".format)
    d.columns = ["Variable", "% que rindió", "Media", "DE", "p25", "p50", "p75"]
d
```

La cobertura es informativa por sí misma: casi todos rinden Competencia Lectora
y Matemática M1, pero **solo el 43% rinde Matemática M2** y la mitad rinde
Historia. Un modelo que use M2 como predictor lo estará imputando para más de la
mitad de los casos.

```{python}
#| label: tbl-paes-dep
#| tbl-cap: "Dependencia del establecimiento de egreso"
d = leer("paes_dependencia")
if d is not None:
    d["proporcion"] = d["proporcion"].map("{:.1%}".format)
    d.columns = ["Dependencia", "n", "Proporción"]
d
```

### Qué años hay, y el corte de escala de 2023 {#sec-paes-escala}

El portal publica los puntajes **desde 2021**, con tres nombres distintos para
la misma base según el año: *Prueba de Transición Universitaria* (2021–2022),
*Prueba de Acceso a la Educación Superior* (2023) y *PAES* (2024 en adelante).
Por eso el descargador lee el catálogo de la página y reconoce por patrón, en
vez de fijar rutas.

```{python}
#| label: tbl-admision-escala
#| tbl-cap: "Instrumento, escala y cobertura por año de admisión"
d = leer("admision_escala")
if d is not None:
    if "Cobertura" in d:
        d["Cobertura"] = d["Cobertura"].map("{:.1%}".format)
    for c in ("Inscritos", "Rindieron", "Mínimo", "Media", "Máximo"):
        if c in d:
            d[c] = d[c].map("{:,.0f}".format)
    d["Año de admisión"] = d["Año de admisión"].astype(str)
d
```

::: {.callout-important}
## Los puntajes no son comparables entre 2022 y 2023

La PDT se medía entre **150 y 850** con media 500; la PAES pasó a **100 y 1000**
con media cercana a 610, y además separó Matemática M1 de M2. Un promedio
institucional salta unos cien puntos entre ambas cohortes sin que haya cambiado
nada real. Cualquier serie que cruce ese corte con el puntaje crudo está
inventando una tendencia.

El pipeline lo resuelve de dos maneras. Reconstruye para la PDT el mismo
promedio que la PAES trae calculado —se verificó que `PROMEDIO_CM_MAX` es
exactamente la media de Competencia Lectora y Matemática, tomando el mejor
puntaje entre el año y el anterior— y expone además
`paes_percentil_promedio`, el percentil nacional dentro de cada cohorte. El
percentil **sí** cruza el corte: quedar sobre el 70% del país significa lo mismo
con cualquiera de las dos pruebas.

Para el agrupamiento de pares el corte es inocuo, porque cada cohorte se
estandariza y se agrupa por separado.
:::

::: {.callout-warning}
## La PSU no está en datos abiertos

La serie **no puede extenderse antes de 2021**. La PSU (2004–2020) no aparece en
la página de admisión ni en ninguna de las 48 secciones del portal del Mineduc;
quien la necesite debe pedirla al DEMRE por su proceso de acceso a datos de
investigación.

La consecuencia práctica es concreta: de las 19 cohortes del benchmark, solo
seis pueden medir selectividad de admisión. En las trece anteriores el bloque se
omite y su peso se reparte entre los demás, de modo que «universidades
comparables» significa allí *ofrece carreras parecidas* pero no necesariamente
*recibe estudiantes parecidos*. La aplicación deja de afirmar que la
selectividad participa cuando no lo hace.
:::

## Trayectoria escolar previa al ingreso

Tres bases adicionales de Mineduc, unidas por `MRUN` al año anterior al ingreso
de cada cohorte:

| Base | Grano | Variables usadas |
|---|---|---|
| Asistencia anual por estudiante | MRUN + RBD, **tasa mensual marzo–diciembre** | Asistencia anual, pendiente, mínimo, último trimestre |
| Alumnos SEP | MRUN + RBD | Prioritario, preferente |
| Asignaciones de Becas y Créditos | MRUN | Quintil, decil DFE, gratuidad, FSCU, beca de arancel |

La variable más interesante no es el nivel de asistencia sino su **pendiente**:
un estudiante que baja de 95% en marzo a 70% en noviembre de 4° medio ya se
estaba desenganchando antes de matricularse. Es el mismo fenómeno que el
proyecto busca detectar dentro del semestre.

```{python}
#| label: tbl-trayectoria
#| tbl-cap: "Trayectoria escolar — cohorte de ingreso 2024"
d = leer("descriptivas_trayectoria_escolar")
if d is not None:
    d["cobertura"] = d["cobertura"].map("{:.1%}".format)
    d.columns = ["Variable", "Cobertura", "Media", "DE", "p25", "p50", "p75"]
d
```

::: {.callout-warning}
## El CAE no está en estas bases

La base de Asignaciones cubre gratuidad, becas de arancel y el Fondo Solidario de
Crédito Universitario. **El CAE lo administra la Comisión Ingresa y se publica
por separado.** Incluir una variable `cae` construida desde esta base daría una
columna constante en cero y llevaría a concluir que "el CAE no predice" cuando
nunca estuvo en los datos.
:::

## OULAD

```{python}
#| label: oulad-resumen
o = resumen.get("oulad", {})
Markdown(f"""
| Métrica | Valor |
|---|---|
| Estudiantes únicos | {o.get('estudiantes_unicos', 0):,} |
| Inscripciones (estudiante × módulo × presentación) | {o.get('inscripciones', 0):,} |
| Módulos | {o.get('modulos', 0)} |
| Presentaciones | {', '.join(o.get('presentaciones', []))} |
| Regiones (Reino Unido) | {o.get('regiones', 0)} |
| Entregas evaluadas | {o.get('entregas_evaluadas', 0):,} |
""")
```

```{python}
#| label: tbl-oulad-resultado
#| tbl-cap: "Distribución del resultado final en OULAD"
r = leer("oulad_resultado_final")
if r is not None:
    r["proporcion"] = r["proporcion"].map("{:.1%}".format)
    r.columns = ["Resultado final", "n", "Proporción"]
r
```

::: {.callout-note}
OULAD es educación **a distancia** del Reino Unido: no existe registro de
asistencia, la escala de notas es 0–100 y los módulos duran ~9 meses. Sus
resultados son un **piso** para lo alcanzable en la UA, no una estimación.
:::

## Datos sintéticos

```{python}
#| label: sint-resumen
s = resumen.get("sintetico", {})
Markdown(f"""
| Métrica | Valor |
|---|---|
| Estudiantes generados | {s.get('estudiantes', 0):,} |
| Asignaturas | {s.get('asignaturas', 0):,} |
| Inscripciones | {s.get('inscripciones', 0):,} |
| Filas del panel semanal | {s.get('filas_panel_semanal', 0):,} |
| Notas parciales | {s.get('notas_parciales', 0):,} |
| Nota final media (escala 1,0–7,0) | {s.get('nota_final_media', 0):.2f} (sd {s.get('nota_final_sd', 0):.2f}) |
| Asistencia media | {pct(s.get('asistencia_media'))} |
| Reprobación por nota | {pct(s.get('reprobacion_por_nota'))} |
| Reprobación por inasistencia | {pct(s.get('reprobacion_por_inasistencia'))} |
| Reprobación total | {pct(s.get('reprobacion_total'))} |
| Cursos sin registro de asistencia | {pct(s.get('cursos_sin_registro_asistencia'))} |
""")
```

```{python}
#| label: tbl-sint-perfil
#| tbl-cap: "Reprobación por perfil latente de estudiante"
p = leer("sint_por_perfil")
if p is not None:
    p["reprobacion"] = p["reprobacion"].map("{:.1%}".format)
    p.columns = ["Perfil latente", "Inscripciones", "Reprobación"]
p
```

```{python}
#| label: tbl-sint-dificultad
#| tbl-cap: "Reprobación por nivel de dificultad de la asignatura"
d = leer("sint_por_dificultad")
if d is not None:
    d["reprobacion"] = d["reprobacion"].map("{:.1%}".format)
    d.columns = ["Dificultad", "Inscripciones", "Reprobación"]
d
```

```{python}
#| label: tbl-sint-sede
#| tbl-cap: "Distribución sintética por sede"
leer("sint_por_sede")
```

---

# Metodología {#sec-metodologia}

## Grano analítico

Todo el pipeline opera sobre **estudiante × asignatura × período**, construido a
partir de un panel semanal **estudiante × asignatura × semana**. La elección no
es cosmética: un modelo entrenado sobre promedios de cierre no distingue a un
estudiante que empezó bien y se cayó de otro que siempre estuvo bajo, y esos dos
casos requieren intervenciones opuestas.

## Prevención de fuga temporal

La fuga de información desde el futuro es el error que **no se detecta mirando
métricas**: produce modelos con AUC excelente que fracasan en producción, porque
en producción el futuro todavía no ocurrió.

Reglas aplicadas:

- Para el scoring de la semana *N*, solo se usan filas con `week <= N`.
- Las notas se filtran por `available_from_week`, **nunca** por `week`. Una nota
  de la semana 8 cargada con atraso en la semana 10 existe en la base con
  `week = 8`; usarla en el scoring de la semana 8 sería entrenar con información
  que en producción no habría estado disponible.
- Ninguna columna del resultado entra al conjunto de features; hay una
  verificación explícita que falla si ocurre.
- Una tabla ausente produce features ausentes (`NaN`), nunca ceros.

La verificación es **de invarianza, no de inspección**: se calculan las features
de la semana 8, se reemplaza con valores arbitrarios todo dato posterior a la
semana 8, y se recalculan. Si algo cambia, el pipeline estaba mirando adelante.

## Validación temporal

Nunca se usa un split aleatorio como criterio principal. Los períodos antiguos
entrenan y los recientes testean. Un split aleatorio mezcla cohortes del mismo
año entre entrenamiento y prueba, y produce métricas optimistas que no se
sostienen sobre una cohorte futura.

## Cohorte fija

Excluir del scoring a quien ya se dio de baja es correcto: no es un caso a
predecir. Pero recalcular esa exclusión en cada semana hace que **la población
cambie** entre puntos de la curva, y entonces una mejora del AUC podría venir de
más información o simplemente de una población distinta.

Por eso la curva de anticipación se calcula sobre una **cohorte fija**, anclada
en la última semana de scoring y evaluada igual en todas. Así lo único que varía
entre filas es la información disponible.

La diferencia se midió: los deltas de AUC entre ambos modos son ≤ 0,008, de modo
que la pendiente **no** venía del cambio de población. Pero la prevalencia
variable (44% → 33%) hacía las filas no comparables, y el lift sí se movía.

## Calibración del generador sintético

El generador **no contiene constantes mágicas**. `config/synthetic.yml` declara
tasas objetivo y el código resuelve sus parámetros internos por bisección para
satisfacerlas. Cambiar el peso de una señal no altera la tasa global de
reprobación, lo que permite probar robustez sin recalibrar todo.

El orden causal es deliberado y no invertible:

```
perfil latente → comportamiento semanal → agregados de cierre
              → riesgo latente → resultado
```

El resultado nunca se sortea primero. Si se hiciera al revés, cualquier modelo
obtendría un AUC excelente y no se sabría si es porque el pipeline funciona o
porque el generador filtró la respuesta.

## Dos objetivos separados

Se modelan por separado `fail_by_grade` (reprobación por nota) y
`fail_by_attendance` (reprobación por inasistencia). Si la universidad reprueba
por inasistencia, la asistencia **no es un predictor** de la reprobación: es
parcialmente el mecanismo reglamentario que la produce. Mezclarlos genera un
modelo que aprendió el reglamento en vez del fenómeno académico.

## Calidad de la plataforma como propiedad del curso

La adopción de Canvas es heterogénea entre docentes. En cursos de baja adopción
las features de actividad se emiten como `NULL` explícito, no como cero: sin
esto, todo estudiante de un curso cuyo profesor no usa la plataforma aparece como
desenganchado.

## Ausencia de dato distinta de cero

Un conteo ausente es un cero real (no hubo tareas, no hubo notas). Un promedio
ausente es **desconocido**. Imputar 0 en `mean_grade` equivale a inventar la peor
nota posible.

---

# Resultados {#sec-resultados}

## Retención real de la Universidad Autónoma {#sec-retencion}

Calculada cruzando Matrícula 2024 con Matrícula 2025 por MRUN. Cohorte de ingreso
2024.

```{python}
#| label: tbl-retencion-resumen
#| tbl-cap: "Retención al segundo año, cohorte 2024"
r = leer("ua_retencion_resumen")
if r is not None:
    r["retencion_misma_carrera"] = r["retencion_misma_carrera"].map("{:.1%}".format)
    r["retencion_sistema"] = r["retencion_sistema"].map("{:.1%}".format)
    r.columns = ["Población", "n", "Misma carrera y universidad", "Cualquier institución"]
r
```

```{python}
#| label: tbl-retencion-sede
#| tbl-cap: "Retención por sede — área Administración y Comercio, cohorte 2024"
r = leer("ua_retencion_por_sede")
if r is not None:
    r = r.sort_values("retencion_misma_carrera", ascending=False)
    r["retencion_misma_carrera"] = r["retencion_misma_carrera"].map("{:.1%}".format)
    r["retencion_sistema"] = r["retencion_sistema"].map("{:.1%}".format)
    r.columns = ["Sede", "n", "Misma carrera", "Cualquier institución"]
r
```

```{python}
#| label: tbl-retencion-carrera
#| tbl-cap: "Retención por carrera — área Administración y Comercio"
r = leer("ua_retencion_por_carrera")
if r is not None:
    r = r.sort_values("retencion_misma_carrera", ascending=False)
    r["retencion_misma_carrera"] = r["retencion_misma_carrera"].map("{:.1%}".format)
    r["retencion_sistema"] = r["retencion_sistema"].map("{:.1%}".format)
    r.columns = ["Carrera", "n", "Misma carrera", "Cualquier institución"]
r
```

::: {.callout-tip}
## Implicación de diseño

La brecha entre la sede con mayor y menor retención supera los **14 puntos**. Con
esos tamaños muestrales no es ruido. La sede debe entrar al modelo como variable,
no quedarse como filtro del tablero.
:::

## Titulación por cohorte de ingreso {#sec-cohortes}

Con la matrícula histórica descargada (2007 en adelante) se puede seguir a cada
cohorte de ingreso contra las bases de titulados de los años siguientes,
cruzando por MRUN. Esto responde una pregunta que los indicadores transversales
no pueden responder: de quienes **entraron** en un año, qué proporción llegó a
titularse. La versión transversal describe a la promoción que **egresa** —cuánto
se demoró quien sí se tituló— y es una cohorte distinta.

Se mide en tres niveles anidados. La brecha entre el primero y el segundo es
cambio de carrera dentro de la misma universidad; entre el segundo y el tercero,
traslado a otra institución. **Sin el tercer nivel, todo traslado se contabiliza
como fracaso.**

```{python}
#| label: tbl-cohortes-nacional
#| tbl-cap: "Titulación por cohorte de ingreso, sistema universitario nacional (plazo = duración nominal + 2 años)"
r = leer("cohortes_titulacion_nacional")
if r is not None:
    for c in ("cobertura", "titulacion_carrera", "titulacion_universidad",
              "titulacion_sistema"):
        r[c] = r[c].map("{:.1%}".format)
    for c in ("ingresantes", "observables"):
        r[c] = r[c].map("{:,}".format)
    r.columns = ["Cohorte", "Ingresantes", "Observables", "Cobertura",
                 "De su carrera", "En su universidad", "En alguna universidad"]
r
```

```{python}
#| label: tbl-cohortes-ua
#| tbl-cap: "Titulación por cohorte de ingreso — Universidad Autónoma de Chile"
r = leer("cohortes_titulacion_ua")
if r is not None:
    for c in ("titulacion_cohorte_cobertura", "titulacion_cohorte_carrera",
              "titulacion_cohorte_universidad", "titulacion_cohorte_sistema"):
        if c in r:
            r[c] = r[c].map(lambda v: "—" if pd.isna(v) else f"{v:.1%}")
    if "titulacion_cohorte_n" in r:
        r["titulacion_cohorte_n"] = r["titulacion_cohorte_n"].map("{:,.0f}".format)
    if "titulacion_cohorte_anios" in r:
        r["titulacion_cohorte_anios"] = r["titulacion_cohorte_anios"].map(
            lambda v: "—" if pd.isna(v) else f"{v:.1f}")
    r.columns = ["Cohorte", "Observables", "Cobertura", "De su carrera",
                 "En su universidad", "En alguna universidad", "Años al título"]
r
```

### Censura por derecha: el problema central

Una cohorte sólo puede evaluarse cuando su plazo se cumplió. Una carrera de cinco
años con dos de holgura necesita siete años de datos posteriores, y de la cohorte
del perfil todavía no se ha titulado nadie. Comparar cohortes sin corregir esto
hace que las recientes parezcan catastróficas por el solo hecho de ser recientes.

El tratamiento tiene **dos niveles**, y el segundo apareció al auditar el
resultado del primero:

1. **Por estudiante.** Cada ingresante entra al denominador sólo si su propio
   horizonte —su duración nominal más la holgura— cabe en los datos disponibles.
   Quien no alcanzó a ser observado ese tiempo queda fuera del denominador, en
   vez de contarse como si hubiera desertado.

2. **Por institución.** Corregir el tiempo no corrige la **composición**. Cuando
   quedan pocos años de seguimiento, las carreras largas caen primero, así que una
   universidad con mucha medicina o ingeniería queda descrita sólo por sus
   carreras cortas y su tasa sube sin que haya titulado mejor. Se exige que al
   menos el 90% de los ingresantes de esa institución sea observable; bajo ese
   umbral la tasa se anula y se conserva la cobertura, que explica por qué.

::: {.callout-warning}
## Este filtro cambió el resultado de la UA, no sólo la teoría

Sin el piso por institución, la UA aparecía **primera del sistema** en la cohorte
2018 con 70,4% y percentil 98. Esa cohorte tenía 81% de observabilidad nacional
pero sólo 80% en la UA, que concentra 15,7% de carreras de seis años o más contra
12,1% del sistema: el filtro le recortaba justamente sus carreras largas. Con el
piso aplicado, la última cohorte comparable pasa a ser **2017**, donde la UA
marca 61,4% frente a una mediana de 50,9%. Sigue siendo una posición alta, pero
es la que resiste auditoría.

La composición de la UA es **más exigente** que la del sistema, no más fácil:
1,1% de sus ingresantes está en carreras de tres años o menos, contra 13,3%
nacional. Su ventaja no viene de ofrecer programas cortos.
:::

::: {.callout-note}
## Una cohorte común para todas

La cohorte de referencia que se une al perfil es **la misma para todas las
universidades**. Si cada una trajera su última cohorte con dato, la más reciente
quedaría representada sólo por las que alcanzan a cerrarla —las de carreras
cortas— y el benchmark compararía a unas instituciones en un año contra otras en
otro. Se exige que la cohorte elegida tenga tasa utilizable en al menos el 80%
del sistema.
:::

### Un error que las tasas anidadas dejaron a la vista

La primera versión tomaba el primer título por clave y recién después descartaba
los anteriores al ingreso. Un título previo de otra carrera ganaba el mínimo y
anulaba al estudiante entero, escondiendo el título posterior que sí correspondía
a esa cohorte. Como el nivel *sistema* agrega más títulos por persona, se
envenenaba más que los otros, y aparecían universidades con titulación en el
sistema **menor** que en su propia institución, lo que es imposible por
construcción.

El orden correcto es descartar por año **antes** de agregar por clave. La
propiedad de anidamiento —carrera ≤ universidad ≤ sistema— se verifica hoy como
test sobre los datos reales, no sólo sobre casos sintéticos.

## Piso predictivo de las variables previas al ingreso

Modelo: regresión logística sobre NEM, ranking, promedio de notas de enseñanza
media, puntajes PAES (Competencia Lectora, Matemática M1 y M2, Historia,
Ciencias), dependencia del establecimiento, sexo y años desde el egreso.
Objetivo: no continuar en la misma carrera al año siguiente.

```{python}
#| label: tbl-piso
#| tbl-cap: "Piso predictivo — comparación de esquemas de validación"
filas = []
n_ = leer("piso_preingreso_nacional")
t_ = leer("piso_preingreso_temporal")
if n_ is not None:
    for _, row in n_.iterrows():
        filas.append({"Validación": "Split aleatorio (una cohorte)",
                      "Población": row["poblacion"], "AUC": f"{row['auc']:.3f}"})
if t_ is not None:
    for _, row in t_.iterrows():
        filas.append({"Validación": "Temporal (2023 entrena → 2024 testea)",
                      "Población": row["poblacion"], "AUC": f"{row['auc']:.3f}"})
pd.DataFrame(filas)
```

Con validación temporal — cohorte 2023 entrena (n = 129.512) y cohorte 2024
testea (n = 135.050), sin que ninguna persona ni ningún año aparezca en ambos
lados:

| Capacidad de revisión | Casos capturados | Sobre azar |
|---|---|---|
| Top 5% | 9,6% | 1,92× |
| Top 10% | 17,6% | 1,76× |
| Top 20% | 31,3% | 1,57× |
| Top 50% | 63,8% | 1,28× |

**El AUC con validación temporal (0,615) es idéntico al del split aleatorio
(0,615).** El piso es estable y el resultado se sostiene sobre cohortes futuras.

## Ablación: ¿cuánto sube el piso con la trayectoria escolar completa?

La pregunta que corresponde antes de incorporar variables socioeconómicas a un
modelo que prioriza estudiantes no es "¿predicen?" sino **"¿cuánto añaden por
encima de lo que ya tenemos?"**. Población: solo universidades, validación
temporal 2023 → 2024.

```{python}
#| label: tbl-ablacion
#| tbl-cap: "Valor incremental de cada bloque de variables previas al ingreso"
a = leer("ablacion_piso")
if a is not None:
    t = a[["modelo", "n_features", "auc", "delta_vs_base", "captura_20"]].copy()
    t["auc"] = t["auc"].map("{:.3f}".format)
    t["delta_vs_base"] = t["delta_vs_base"].map("{:+.3f}".format)
    t["captura_20"] = t["captura_20"].map("{:.1%}".format)
    t.columns = ["Modelo", "N° features", "AUC", "Δ vs base", "Top 20%"]
t
```

Tres lecturas:

**La asistencia de enseñanza media aporta +0,005.** Esto no contradice a ULagos:
lo que ese informe mostró es que la asistencia importa *dentro* de la
institución y del período en curso. Como rasgo que se arrastra a través de una
transición institucional, casi no transfiere — de hecho, **usada sola alcanza
AUC 0,543**, apenas sobre el azar.

**Las variables socioeconómicas aportan +0,014.** Es poco, y ese "poco" resuelve
empíricamente el dilema ético: excluirlas del scoring cuesta casi nada en
desempeño. La decisión de usarlas o no deja de ser un intercambio entre equidad y
precisión.

**Todo junto llega a 0,636.** Con el expediente escolar íntegro —asistencia
mensual, decil de ingreso oficial, condición de prioritario, tipo de
financiamiento— el piso sigue por debajo de lo que un baseline conductual
alcanza en la semana 4.

## Curva de anticipación sobre OULAD

```{python}
#| label: tbl-oulad-leadtime
#| tbl-cap: "Poder predictivo vs anticipación — OULAD, cohorte fija"
m = leer("metricas_oulad")
if m is not None:
    t = m[["week", "auc", "lift_10", "lift_20", "weeks_remaining"]].copy()
    t["auc"] = t["auc"].map("{:.3f}".format)
    for c in ("lift_10", "lift_20"):
        t[c] = t[c].map("{:.1%}".format)
    t.columns = ["Semana", "AUC", "Top 10%", "Top 20%", "Semanas restantes"]
t
```

## Curva de anticipación sobre datos sintéticos

```{python}
#| label: tbl-sint-leadtime
#| tbl-cap: "Poder predictivo vs anticipación — datos sintéticos"
m = leer("metricas_synthetic")
if m is not None:
    t = m[["week", "auc", "lift_10", "lift_20", "weeks_remaining"]].copy()
    t["auc"] = t["auc"].map("{:.3f}".format)
    for c in ("lift_10", "lift_20"):
        t[c] = t[c].map("{:.1%}".format)
    t.columns = ["Semana", "AUC", "Top 10%", "Top 20%", "Semanas restantes"]
t
```

Estas cifras **no son un hallazgo**: miden que el pipeline recupera la señal que
el propio generador inyectó, y que la dificultad del problema simulado está en el
rango que reporta la literatura.

---

# El argumento central {#sec-argumento}

Poniendo los dos resultados reales uno junto al otro:

| Modelo | Datos | n | AUC |
|---|---|---|---|
| Ficha de admisión | PAES + Matrícula, validación temporal | 135.050 | 0,617 |
| **Expediente escolar completo** | + asistencia mensual, decil DFE, SEP, financiamiento | 135.050 | **0,636** |
| **Comportamiento, semana 4** | OULAD, 10% del curso, **sin asistencia** | 13.144 | **0,697** |
| Comportamiento, semana 12 | OULAD, 31% del curso, sin asistencia | 13.144 | 0,801 |

> Todo lo que el Estado de Chile sabe de un estudiante antes de que pise la
> universidad —NEM, ranking, cinco pruebas PAES, dependencia del colegio,
> asistencia mensual de 4° medio, decil de ingreso oficial, condición de
> prioritario, tipo de financiamiento— alcanza **AUC 0,636**.
>
> Un baseline conductual lo supera con el **10% del curso transcurrido**, y sin
> siquiera disponer de asistencia — justamente la variable que el informe ULagos
> identificó como clave.

Ese es el caso para solicitar acceso a Banner y Canvas: **el valor no está en lo
que ya se sabe del estudiante al matricularse.**

---

# Limitaciones y supuestos abiertos {#sec-limitaciones}

## Sobre los datos

- **Cobertura del cruce PAES.** Solo el 53% de los ingresantes 2024 aparece en
  PAES 2024: quienes entran a IP/CFT o por vías alternativas no la rinden. La
  población analizada son ingresantes con PAES, que es el perfil relevante para
  la UA pero no describe al sistema completo.
- **OULAD no tiene asistencia.** Sus resultados son un piso.
- **OULAD no registra fecha de corrección**, solo de entrega. Se asume
  disponibilidad inmediata, lo cual es optimista.
- **Los datos sintéticos no son evidencia** sobre estudiantes reales.

## Sobre el sesgo

Las fuentes unibles por RBD (IVE, SIMCE, dependencia) son **proxies de nivel
socioeconómico**. Un modelo que marque en rojo a estudiantes de colegios
vulnerables *antes de observar conducta alguna* no detecta riesgo: reproduce
desigualdad.

La recomendación fue someterlas a una prueba de **valor incremental**, y esa
prueba ya se corrió (§ @sec-resultados): las variables socioeconómicas —quintil,
decil DFE, prioritario, financiamiento— aportan **+0,014 de AUC** sobre la ficha
de admisión.

Eso convierte una discusión ética en una decisión barata: **excluirlas del
scoring cuesta casi nada en desempeño**. La recomendación operativa es usarlas
para el análisis de equidad y para dimensionar la focalización, y mantenerlas
fuera del modelo que prioriza estudiantes. La decisión final es de la
universidad, no técnica, y conviene plantearla en el mismo taller donde se
prioricen los casos de uso.

## Preguntas bloqueantes para la universidad

Tres cambian el diseño del pipeline:

1. **¿Usa la UA el módulo Banner Attendance Tracking?** Es opcional. Sin él solo
   existe `SFRSTCR_LAST_ATTEND`, que no entrega grano semanal.
2. **¿Existen notas parciales fechadas**, o solo la nota final del período?
3. **¿Contempla el reglamento la reprobación por inasistencia**, y con qué
   umbral?

El contrato completo de datos, con el nivel de confianza de cada campo, está en
`config/data_contracts.yml`.

---

# Marco legal {#sec-legal}

La **Ley 21.719** de Protección de Datos Personales entra en plena vigencia el
**1 de diciembre de 2026**, dentro del horizonte de este proyecto. Consagra el
derecho a no ser objeto de decisiones basadas exclusivamente en tratamiento
automatizado, incluido el perfilamiento, con garantías de **explicación,
intervención humana y revisión**.

Consecuencias de diseño, no de cumplimiento:

1. El humano en el circuito deja de ser buena práctica y pasa a ser requisito: el
   modelo prioriza, la dirección de carrera decide.
2. La explicabilidad por caso deja de ser opcional.
3. Conviene levantar base de licitud y evaluación de impacto con la Dirección
   Jurídica **antes** de que el modelo toque datos reales.

---

# Reproducibilidad {#sec-reproducibilidad}

Todas las cifras de este informe se regeneran con:

```bash
python -m venv .venv && .venv\Scripts\activate
pip install -e ".[modeling,ui,dev]"

python scripts/generate_synthetic.py
python scripts/download_oulad.py
python scripts/download_mineduc.py
python scripts/analyze_mineduc.py
python scripts/validate_lead_time.py --source synthetic --out data/results
python scripts/validate_lead_time.py --source oulad     --out data/results
python scripts/descriptive_stats.py

quarto render documentacion/informe_metodologico.qmd
```

El generador sintético es determinista dado su semilla
(`config/synthetic.yml: seed`). La suite de pruebas incluye una verificación de
reproducibilidad que falla si dos ejecuciones con la misma semilla difieren.

```{python}
#| label: entorno
import sys, platform
Markdown(f"""
| | |
|---|---|
| Python | {sys.version.split()[0]} |
| Plataforma | {platform.system()} {platform.release()} |
| pandas | {pd.__version__} |
| Generado | {pd.Timestamp.now():%Y-%m-%d %H:%M} |
""")
```
